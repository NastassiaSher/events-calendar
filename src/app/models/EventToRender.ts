export class EventToRender {
  title;
  subtitle;
  x;
  y;
  width;
  height;
  constructor(title, subtitle, x, y, width, height) {
    this.title = title;
    this.subtitle = subtitle;
    this.x = x;
    this.y = y;
    this.width = width;
    this.height = height;
  }
}
